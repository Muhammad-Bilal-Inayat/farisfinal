import { db } from "./src/db/index";
import { vehicles } from "./src/db/schema";

async function updateDefaultVehicles() {
  console.log("Updating default vehicles in code / seed...");
  // We can write a new seed file or update seed.ts
}
updateDefaultVehicles();
